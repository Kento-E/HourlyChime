<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Bluetooth: API 30 以下は BLUETOOTH のみ、API 31+ は BLUETOOTH_CONNECT が必要 -->
    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HourlyChime" >
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.HourlyChime" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <activity
            android:name=".ShortcutActionActivity"
            android:exported="true"
            android:theme="@android:style/Theme.NoDisplay" />

        <receiver
            android:name=".TimeSignalReceiver"
            android:exported="false" >
            <intent-filter>
                <action android:name="com.example.hourlychime.ACTION_TIME_SIGNAL" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".BootReceiver"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Bluetooth接続状態監視用レシーバー（省電力化） -->
        <receiver
            android:name=".BluetoothEventReceiver"
            android:exported="false" >
            <intent-filter>
                <action android:name="android.bluetooth.adapter.action.STATE_CHANGED" />
                <action android:name="android.bluetooth.adapter.action.CONNECTION_STATE_CHANGED" />
            </intent-filter>
        </receiver>

        <!-- 外部操作用レシーバー（Galaxyルーチン / Tasker など） -->
        <receiver
            android:name=".ControlReceiver"
            android:exported="true" >
            <intent-filter>
                <action android:name="com.example.hourlychime.ACTION_TOGGLE_ENABLED" />
                <action android:name="com.example.hourlychime.ACTION_SET_TIME_RANGE" />
                <action android:name="com.example.hourlychime.ACTION_SET_ENABLED_DAYS" />
                <action android:name="com.example.hourlychime.ACTION_SET_BLUETOOTH_FILTER" />
                <action android:name="com.example.hourlychime.ACTION_SET_SKIP_HOLIDAYS" />
            </intent-filter>
        </receiver>

        <!-- Galaxyルーチン「アプリの操作を実行」対応用サービス -->
        <service
            android:name=".QuickActionService"
            android:exported="true" >
            <intent-filter>
                <action android:name="com.example.hourlychime.ACTION_TOGGLE_TIME_SIGNAL" />
                <action android:name="com.example.hourlychime.ACTION_TURN_ON" />
                <action android:name="com.example.hourlychime.ACTION_TURN_OFF" />
            </intent-filter>
        </service>

        <!-- Galaxyルーチン用QuickAction定義Activity -->
        <activity
            android:name=".QuickActionActivity"
            android:exported="true"
            android:theme="@android:style/Theme.NoDisplay" >
            <intent-filter>
                <action android:name="android.intent.action.CREATE_SHORTCUT" />
                <action android:name="android.app.action.DEFINE_QUICKACTION" />
            </intent-filter>
        </activity>
    </application>

</manifest>